<?xml version="1.0" standalone="yes"?>
<!-- build configuration -->
<project name="thane" default="dist" basedir=".">

  <!-- import overriding properties -->
  <property file="build.properties"/>

  <!-- configuration parameters -->
  <property name="lib.name"     value="thane"/>
  <property name="deploy.dir"   value="dist"/>
  <property name="savedoc.dir"  value="docs"/>
  <property name="javadoc.home" value="${deploy.dir}/docs"/>
  <property name="classes.dir"  value="${deploy.dir}/classes"/>
  <property name="libs.dir"     value="extlibs/java"/>
  <import file="etc/libs-incl.xml"/>

  <property name="tamarin.dir"  value="${basedir}/tamarin-central"/>
  <property name="tamdist.dir"  value="${tamarin.dir}/dist"/>
  <property name="thane.dir"    value="${tamarin.dir}/thane"/>
  <property name="builtin.name" value="builtin"/>
  <property name="http.name"    value="http"/>

  <!-- declare our classpath business -->
  <path id="classpath">
    <fileset dir="${deploy.dir}/lib" includes="**/*.jar"/>
  </path>

  <!-- prepares the application directories -->
  <target name="prepare">
    <mkdir dir="${deploy.dir}"/>
    <mkdir dir="${deploy.dir}/lib"/>
    <mkdir dir="${tamdist.dir}"/>
    <copy todir="${deploy.dir}/lib" flatten="true">
      <fileset refid="${lib.name}.libs"/>
    </copy>
    <copy file="etc/thane-config.xml.in" tofile="${deploy.dir}/thane-config.xml">
      <filterset>
        <filter token="ooothane" value="${basedir}"/>
      </filterset>
    </copy>
  </target>

  <!-- checks whether the Thane library needs building -->
  <target name="checkthanelib">
    <condition property="no_build_thanelib"><and>
      <uptodate targetfile="${thane.dir}/${lib.name}.abc">
        <srcfiles dir="${thane.dir}" includes="**/*.as"/>
        <srcfiles dir="${tamarin.dir}/core" includes="*.abc"/>
      </uptodate>
      <uptodate targetfile="${deploy.dir}/${lib.name}.swc">
        <srcfiles dir="${thane.dir}" includes="**/*.as"/>
        <srcfiles dir="${tamarin.dir}/core" includes="*.as"/>
      </uptodate>
    </and></condition>
  </target>

  <!-- builds the Thane library -->
  <target name="thanelib" unless="no_build_thanelib" depends="prepare,checkthanelib">
    <taskdef name="asclib" classname="flex.ant.AscLibTask" classpathref="classpath"/>
    <dirname property="abs.thane.out.dir" file="${tamarin.dir}/thane/somefile.txt"/>
    <asclib as3="true" strict="true" classpathref="classpath"
            outdir="${abs.thane.out.dir}" out="thane">
      <import dir="${tamarin.dir}">
        <include name="core/builtin.abc"/>
      </import>
      <filespec dir="${tamarin.dir}/thane/as3src">
        <include name="**/*.as"/>
      </filespec>
    </asclib>

    <java jar="flexsdk/lib/compc.jar" fork="true" failonerror="true">
      <arg value="-load-config"/>
      <arg value="dist/thane-config.xml"/>
      <arg value="-compiler.strict=false"/>
      <arg value="-compiler.show-actionscript-warnings=false"/>
      <arg value="-compiler.optimize=true"/>
      <arg value="-include-sources"/>
      <arg value="${tamarin.dir}/core/builtin.as"/>
      <arg value="${tamarin.dir}/core/Error.as"/>
      <arg value="${tamarin.dir}/core/Math.as"/>
      <arg value="${tamarin.dir}/core/XML.as"/>
      <arg value="${tamarin.dir}/core/Date.as"/>
      <arg value="${tamarin.dir}/core/RegExp.as"/>
      <arg value="${thane.dir}/as3src"/>
      <arg value="-output"/>
      <arg value="${deploy.dir}/${lib.name}.swc"/>
    </java>
  </target>

  <!-- checks whether our HTTP library needs building -->
  <target name="checkhttplib">
    <condition property="no_build_httplib">
      <uptodate targetfile="${deploy.dir}/http.swc">
        <srcfiles dir="${deploy.dir}" includes="*.swc"/>
        <srcfiles dir="http/src" includes="**/*.as"/>
      </uptodate>
    </condition>
  </target>

  <!-- builds the HTTP library -->
  <target name="httplib" unless="no_build_httplib" depends="thanelib,checkhttplib">
    <java jar="flexsdk/lib/compc.jar" fork="true" failonerror="true">
      <arg value="-load-config"/>
      <arg value="dist/thane-config.xml"/>
      <arg value="-compiler.external-library-path"/>
      <arg value="${deploy.dir}/thane.swc"/>
      <arg value="-include-sources=http/src"/>
      <arg value="-output"/>
      <arg value="${deploy.dir}/${http.name}.swc"/>
    </java>
  </target>

  <!-- build Tamarin -->
  <condition property="isunix"><os family="unix"/></condition>
  <if><os name="FreeBSD"/><then>
    <property name="make" value="gmake"/>
  </then><else>
    <property name="make" value="make"/>
  </else></if>
  <property name="tamarin-debugger" value=""/>
  <target name="tamarin" depends="prepare,thanelib" if="isunix">
    <echo level="info" message="Doing native compilation on ${os.name}..."/>
    <exec dir="${tamdist.dir}" executable="python" failonerror="true">
       <arg line="${tamarin.dir}/configure.py --enable-thane ${tamarin-debugger} --enable-debug"/>
    </exec>
    <exec dir="${tamdist.dir}" executable="${make}" failonerror="true">
       <arg line="-j4"/>
    </exec>
  </target>

  <!-- tests the HTTP library -->
  <target name="http-test" unless="no_build_http_test" depends="tamarin,httplib">
    <java jar="flexsdk/lib/mxmlc.jar" fork="true" failonerror="true">
      <arg value="-load-config"/>
      <arg value="dist/thane-config.xml"/>
      <arg value="-compiler.external-library-path"/>
      <arg value="${deploy.dir}/thane.swc"/>
      <arg value="${deploy.dir}/http.swc"/>
      <arg value="-output"/>
      <arg value="${deploy.dir}/http-test.swf"/>
      <arg value="http/test/GameTest.as"/>
    </java>

    <java outputproperty="dump" jar="flexsdk/lib/swfdump.jar" fork="true" failonerror="true">
      <arg value="-dump"/>
      <arg value="${deploy.dir}/http-test.abc"/>
      <arg value="${deploy.dir}/http-test.swf"/>
    </java>

    <exec executable="${tamdist.dir}/thane/avmthane">
      <arg line="-Dinterp ${deploy.dir}/http.abc ${deploy.dir}/http-test.abc -- http://www.alyx.com/zell/gameserver.abc"/>
    </exec>
  </target>

  <!-- cleans up our build artifacts -->
  <target name="clean">
    <!-- TODO -->
  </target>

  <!-- wipes out our build directory and deletes all non-version-controlled files -->
  <target name="distclean">
    <delete dir="${deploy.dir}"/>
    <delete dir="${tamarin.dir}/${deploy.dir}"/>
    <delete><fileset dir="${tamarin.dir}/build" includes="*.pyc"/></delete>
    <delete><fileset dir="${tamarin.dir}/thane" includes="thane.*"/></delete>
  </target>

  <!-- builds our distribution files (war and jar) -->
  <target name="dist" depends="prepare,thanelib,tamarin,httplib">
  </target>

  <!-- deploys our built libraries to maven -->
  <target name="deploy" depends="dist">
    <antcall target="maven-deploy"/>
  </target>

</project>
