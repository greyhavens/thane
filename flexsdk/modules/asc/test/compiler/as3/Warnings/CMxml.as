/**
 *	
 *  	Coach Warning for  XML class: Location and name changed. Moved to flash.xml package,  * and name changed to XMLDocument to avoid conflict with the new top level XML class that  * implements ECMAScript for XML (E4X). 
 */

package Warnings{

    var my_xml:XML = new XML()
}