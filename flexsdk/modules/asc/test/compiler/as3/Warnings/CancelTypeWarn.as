/**
 *	
 *  	Warning Message: Use the 'EventType.CANCEL' const instead.
 */

package Warnings {
        import flash.events.Event;
	import flash.events.EventType;
        import flash.util.trace;	
	
        trace(Event.CANCEL_TYPE);
	trace(EventType.CANCEL_TYPE);
			
}

